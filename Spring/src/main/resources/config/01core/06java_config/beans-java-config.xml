<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- 
		@Bean注解
		@Bean注解标识在方法上，表示该方法返回的对象将会被spring容器管理，方法内不仅可以包含对象的构建过程，还可以包含初始化等过程。类似于XML配置中的<bean />元素，最终被解析为bean definitions。
		@Bean注解可以和任意@Component注解搭配，但一般和@Configuration注解一起使用。
	 -->
	 
	<!-- 
		@Configuration注解
		@Configuration注解标识在类上，表示该类作为Bean Definitions的源。类似于XML配置中的<beans />元素。
		@Congiguration标识的类也会向其中的@Bean一样，最终被解析为Bean Definition。
	 -->
	 
	 <!-- 
	 	基于java配置的Spring需要使用AnnotationConfigApplicationContext构建Spring容器。
	  -->
	  
	 <!-- 
	 	类似的，基于java方式的配置也有导入别的配置类。@Import注解类似于XML配置中的 <import />元素。
	  -->
	
</beans>